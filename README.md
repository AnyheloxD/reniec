# Plugin WordPress: Autenticación con RENIEC (IDAAS Perú)

Este plugin permite generar un botón de autenticación con **RENIEC (IDAAS Perú)** utilizando el SDK oficial y redirige al usuario a una URL con parámetros firmados sin necesidad de procesar la respuesta.

